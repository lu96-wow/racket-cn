#lang info

(define collection "racket-cn/ffi")

(define version "0.2.0")

(define title "racket-cn/ffi — FFI 中文别名")

(define authors '("debian"))

(define description
  (string-append
   "racket-cn 的 FFI 子模块。\\n"
   "对应 Racket 的 ffi 集合。\\n"
   "提供 ffi/unsafe 等模块的中文别名。"))

(define deps '("racket-cn" "base"))

(define build-deps '())

(define auto-tests '())
