#lang info

(define collection "racket-cn/json")

(define version "0.1.0")

(define title "racket-cn/json — JSON 处理中文别名")

(define authors '("debian"))

(define description
  (string-append
   "racket-cn 的 json 子模块。\\n"
   "对应 Racket 的 json 库。\\n"
   "提供 json-null, jsexpr?, write-json, read-json 等中文别名。"))

(define deps '("base"))

(define build-deps '())

(define auto-tests '())
